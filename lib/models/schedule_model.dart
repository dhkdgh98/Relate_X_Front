class Schedule {
  final DateTime dateTime;
  final String title;
  final String description;

  Schedule({
    required this.dateTime,
    required this.title,
    required this.description,
  });
}
